# 2.2 Rethinking / Iterative Reasoning 段落蓝图

## 段落任务

1. 保留操作性定义与公式，补充“语义阶段可作为迭代状态，但不能把任意 token 数量都计作精化轮数”的判据。
2. 用 ECoT 展示动作前的自回归具身推理链：计划、子任务、运动、视觉落地状态逐步进入动作生成，不经过独立 verifier。
3. 用 CoA-VLA 展示视觉—文本 affordance reasoning 子链，并明确其最终 diffusion action head 需要组合标注。
4. 用 ISR-LLM 作为反例，说明 self-refinement 的论文命名不能覆盖实际的 validator 数据流。

## 证据—段落对应

| Paragraph / case | Main claim | Evidence | Classification result |
| --- | --- | --- | --- |
| 判据补充 | 可检查的语义阶段是 `h⁽ʳ⁾`，普通底层 token 不自动构成新 refinement round | REASON-LOCAL | 保留本文操作性定义 |
| ECoT | 多步具身推理在动作前自回归展开并直接条件化动作 | ECoT | `Depth × Rethinking / Iterative Reasoning` |
| CoA-VLA | 有序 affordance reasoning 条件化 diffusion action generation | CoA | `Depth × Rethinking / Iterative Reasoning + Generative Refinement` |
| 边界句 | validator 对初始计划作评价和修正 | ISR | `Verifier-Guided Refinement`，不纳入 2.2 正例 |

## 写作约束

- 不把论文作者的 CoT 命名写成领域统一 taxonomy。
- 不把 ECoT 描述为显式错误纠正；它是语义推理链的顺序展开。
- 不把 CoA-VLA 的 diffusion action head 抹去，索引采用组合标签。
- 不为 ISR-LLM 编造纯 self-reasoning 机制；validator 是其归类依据。
- 论文案例初次补充时未增加图片；根据用户后续要求，现从正式 PDF 提取 ECoT Figure 3/4 与 CoA-VLA Figure 1/2，并增加分类解读。

## Figure slots

| Slot | Source figure | Role in argument | Local asset |
| --- | --- | --- | --- |
| ECoT-F3 | ECoT Figure 3 | 展示 `TASK → PLAN → SUBTASK → MOVE → grounding → ACTION` 的单条语义推理链 | `ecot-fig3-reasoning-steps.png` |
| ECoT-F4 | ECoT Figure 4 | 区分训练数据合成工具链与部署时 reasoning transition | `ecot-fig4-data-pipeline.png` |
| CoA-F1 | CoA-VLA Figure 1 | 展示 affordance reasoning、visual-textual co-injection 与 diffusion action head 的组合数据流 | `coa-vla-fig1-framework.png` |
| CoA-F2 | CoA-VLA Figure 2 | 展示任务执行过程中动态选择必要 affordance，并防止把真实时间轴误算为同一决策的 $R$ | `coa-vla-fig2-affordance-chain.png` |

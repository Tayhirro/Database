
- 匈牙利算法
```cpp
#include<iostream>
#include<vector>
using namespace std;
bool find(int num, vector<int>& evens, vector<bool>& used, vector<int>& match){
    for(int i = 0; i < evens.size(); i++){ //遍历每个偶数与奇数比较
        if(isprime(num + evens[i]) && !used[i]){
            used[i] = true;
            if(match[i] == 0 || find(match[i], evens, used, match)){ //如果第i个偶数还未配对，或者跟它配对的奇数还有别的选择
                match[i] = num; //则配对该数
                return true;
            }
        }
    }
    return false;
}
int main(){
        vector<int> match(evens.size(), 0); //统计每个偶数的配对是哪个奇数
        for(int i = 0; i < odds.size(); i++){ //遍历每个奇数
            vector<bool> used(evens.size(), false); //每一轮偶数都没用过
            if(find(odds[i], evens, used, match)) //能否找到配对的偶数，且要最优
                count++;
        }
        cout << count << endl;
    }
    return 0;
}
```

# 二叉树（Binary Tree）

## 一句话
二叉树是每个节点最多有两个子树（左子树和右子树）的树结构。

## 严格定义
二叉树 $T$ 是一个有限的节点集合，该集合或者为空，或者由一个根节点以及两棵互不相交的、分别称为左子树 $T_L$ 和右子树 $T_R$ 的二叉树组成。

## 遍历（Traversal）
遍历是按特定规则访问树中所有节点各一次的操作。

### 1. 前序遍历 (Preorder Traversal)
**顺序**：根节点 $\to$ 左子树 $\to$ 右子树。

**迭代实现（显式栈）**：
```cpp
vector<int> preorderTraversal(TreeNode* root) {
    vector<int> res;
    if (root == nullptr) return res;
    stack<TreeNode*> stk;
    stk.push(root);
    while (!stk.empty()) {
        TreeNode* node = stk.top();
        stk.pop();
        res.push_back(node->val);
        if (node->right) stk.push(node->right); // 先压右，后出左
        if (node->left) stk.push(node->left);
    }
    return res;
}
```
*注：另一种模拟递归堆栈的写法（模板式）：*
```cpp
vector<int> preorderTraversal(TreeNode* root) {
    vector<int> res;
    stack<TreeNode*> stk;
    TreeNode* node = root;
    while (node != nullptr || !stk.empty()) {
        while (node != nullptr) {
            res.push_back(node->val); // 访问
            stk.push(node);
            node = node->left;
        }
        node = stk.top(); stk.pop();
        node = node->right;
    }
    return res;
}
```

### 2. 中序遍历 (Inorder Traversal)
**顺序**：左子树 $\to$ 根节点 $\to$ 右子树。

**迭代实现（显式栈）**：
```cpp
vector<int> inorderTraversal(TreeNode* root) {
    vector<int> res;
    stack<TreeNode*> stk;
    TreeNode* node = root;
    while (node != nullptr || !stk.empty()) {
        while (node != nullptr) {
            stk.push(node);
            node = node->left;
        }
        node = stk.top(); stk.pop();
        res.push_back(node->val); // 访问
        node = node->right;
    }
    return res;
}
```

### 3. 后序遍历 (Postorder Traversal)
**顺序**：左子树 $\to$ 右子树 $\to$ 根节点。

**迭代实现（显式栈）**：
*技巧：前序是“根左右”，调整为“根右左”，最后反转即为“左右根”。*
```cpp
vector<int> postorderTraversal(TreeNode* root) {
    vector<int> res;
    if (root == nullptr) return res;
    stack<TreeNode*> stk;
    stk.push(root);
    while (!stk.empty()) {
        TreeNode* node = stk.top();
        stk.pop();
        res.push_back(node->val);
        if (node->left) stk.push(node->left);   // 先压左，后出右
        if (node->right) stk.push(node->right);
    }
    reverse(res.begin(), res.end());
    return res;
}
```
*通用迭代写法（防止反转）：*
```cpp
vector<int> postorderTraversal(TreeNode* root) {
    vector<int> res;
    stack<TreeNode*> stk;
    TreeNode *prev = nullptr;
    TreeNode *node = root;
    while (node != nullptr || !stk.empty()) {
        while (node != nullptr) {
            stk.push(node);
            node = node->left;
        }
        node = stk.top(); stk.pop();
        if (node->right == nullptr || node->right == prev) {
            res.push_back(node->val); // 访问
            prev = node;
            node = nullptr; // 强制退栈
        } else {
            stk.push(node); // 再次入栈
            node = node->right;
        }
    }
    return res;
}
```

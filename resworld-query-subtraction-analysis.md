## ResWorld (TR-World): Query 相减是否只是等价变换

本质：
- Query 相减（$\Delta Q_t = Q_t - Q_{t-1}$）在信息论意义上是可逆的保体积线性变换，但在优化动力学和有限容量模型的归纳偏置意义上**不是**等价变换。它是一种**任务感知的结构化信息重构**——通过外化差分计算和静态信息过滤，将模型的有限容量重新分配到动态预测这一任务相关信息上。

- **通用分析骨架**
  - **对象与边界**：
    - 判定对象：从 $Q_t$ 到 $\Delta Q_t = Q_t - Q_{t-1}$ 的映射是否构成等价变换。
    - "等价变换"需在三个层面分别定义：信息论等价（双射/可逆）、函数空间等价（假设空间不变）、优化等价（梯度流和收敛行为相同）。
    - 不研究 ResWorld 的其他模块（FGTR 轨迹精修、TokenFuser）。

  - **核心问题**：
    - 待判定命题："query 相减只是对 query 的等价变换"。
    - 这是一个判定/评价型问题，需要从信息论、优化理论、信号处理、实验证据四个维度分别评判。

  - **依据与可信度**：
    - 信息论等价：数学定理直接支持（差分矩阵行列式为 1）。
    - 优化不等价：Amari (1998) 自然梯度理论、Santurkar et al. (2018, NeurIPS) BN 景观分析、Salimans & Kingma (2016) Weight Normalization——均为文献直接支持。
    - 高通滤波效应：Oppenheim & Schafer (2010) 数字信号处理标准结论——文献直接支持。
    - 预测编码类比：Rao & Ballard (1999) 视觉皮层预测编码、Friston (2005, 2010) 自由能原理——文献直接支持预测误差不是等价变换，但映射到 ResWorld 场景属综合推断。
    - 消融实验：论文原文数据（L2: 0.59m vs 0.61m, 碰撞率: 0.17 vs 0.21）——文献直接支持。
    - 信息瓶颈分析：Tishby et al. (1999) 框架为文献直接支持，将其映射到 query 相减属综合推断。

  - **适用范围与失败边界**：
    - "不等价"结论在有限模型容量、有限训练预算、任务聚焦动态预测的条件下成立。
    - 在模型容量趋近无穷 + 训练时间趋近无穷的极限下，两者可能收敛到同一函数（但这一极限在工程实践中不可达）。
    - 当坐标对齐（ego-motion compensation）精度不足时，差分可能引入伪影，反而不如不做。
    - 在全动态场景（所有物体都在剧烈运动）中，差分的稀疏化优势消失。

- **问题—分析—映射**

  **信息论等价性与条件可逆性**

  差分变换 $(Q_0, Q_1, \ldots, Q_T) \mapsto (Q_0, \Delta Q_1, \ldots, \Delta Q_T)$ 的变换矩阵 $D$ 是下三角块矩阵，对角块均为单位矩阵，因此 $\det(D) = 1$。这是一个保体积的双射映射，逆矩阵为累加矩阵（下三角全 1 矩阵）。在信息论和通用逼近能力的意义上，query 相减确实是等价变换——假设空间不变，信息无损失（前提是保留 $Q_0$ 作为绝对参考）。然而，从信号处理角度看，如果不保留初始状态 $Q_0$ 而仅使用差分序列 $\{\Delta Q_t\}$，则操作变为有损的：一阶差分的传递函数 $H(z) = 1 - z^{-1}$ 在直流分量处（$\omega = 0$）增益为零，意味着静态场景的绝对信息被完全消除。ResWorld 的实际做法是将差分序列送入世界模型、同时将当前帧 BEV 特征（包含静态信息）通过 TokenFuser 模块在下游融合，因此信息在系统级别被保留，但在差分操作本身是选择性丢弃的。这一判断由 Oppenheim & Schafer (2010) 的标准滤波器理论和 Cover & Thomas (2006) 的条件熵分析直接支持。

  **优化不等价：重参数化改变梯度流与 Hessian 条件数**

  这是三个独立调研方向最核心的共识。Amari (1998) 的自然梯度理论证明了一个关键定理：标准梯度下降不是重参数化不变的（reparameterization-invariant）——即使两个参数化通过双射关联，它们在参数空间中的 Fisher 信息度量不同，导致梯度流方向完全不同。具体到 query 相减，差分矩阵 $D$ 不是正交矩阵（$DD^T \neq I$），因此变换后的梯度流为 $\dot{\phi} = -DD^T \nabla_\phi L$，方向发生了改变。Hessian 也发生了变换：$H_\theta = D^T H_\phi D$，条件数随之改变。Santurkar et al. (2018, NeurIPS) 对 Batch Normalization 的分析提供了最直接的类比——BN 不改变模型的表达能力，但通过使 loss landscape 更加 $\beta$-smooth 来显著加速训练。Query 相减的作用机制与此同构：它是对输入的线性预处理，不改变信息内容，但改变了优化景观的几何特性，使得模型更容易学到动态变化的模式。Salimans & Kingma (2016) 的 Weight Normalization（方向-尺度解耦）和 Neyshabur et al. (2015) 的 Path-SGD（正缩放不变参数化）进一步从不同侧面佐证了"信息等价 ≠ 优化等价"这一在 ML 优化理论中已被广泛接受的原则。

  **高通滤波与预测编码：差分作为信息选择机制**

  一阶差分 $\Delta Q_t = Q_t - Q_{t-1}$ 的幅度响应为 $|H(e^{j\omega})| = 2|\sin(\omega/2)|$，这是一个经典的高通滤波器：完全消除直流分量（静态场景）、衰减低频成分（缓慢变化）、保留并增强高频成分（快速运动）。在自动驾驶场景中，经过 ego-motion compensation 后，静态背景在相邻帧之间几乎不变，差分后接近零；而运动物体引起的表征变化被保留。这不仅是优化偏置，更是一种**有意的信息过滤**——Rao & Ballard (1999) 的预测编码理论明确指出，视觉皮层传递的不是原始信号而是预测误差（残差），因为残差的熵 $H(e) \ll H(x)$ 远低于原始信号，是一种高效的冗余消除策略（Barlow, 1961）。Friston (2005, 2010) 的自由能原理进一步将预测误差提升为大脑更新内部模型和驱动行为的核心信号。ResWorld 的差分操作可以被理解为使用零阶保持预测的简化版预测编码——用 $Q_{t-1}$ 作为对 $Q_t$ 的预测，$\Delta Q_t$ 即为预测误差。与卡尔曼滤波的新息过程（Kalman, 1960; Kailath, 1968）相比，ResWorld 的差分使用更简单的零阶保持预测而非最优预测，因此残差中仍包含可进一步预测的成分（如匀速运动），但计算代价更低。这些分析共同指向：query 相减不是等价变换，而是一种有损但有效的信息选择机制。

  **消融实验与架构对比：实践层面的否定证据**

  ResWorld 的消融数据显示，query 相减模型相比直接使用完整 query 的标准世界模型，L2 误差从 0.61m 降至 0.59m（改善 3.3%），碰撞率从 0.21 降至 0.17（改善 19%）。两个指标的不对称改善值得注意：碰撞率的大幅改善暗示 query 相减的获益主要集中在长尾的高风险交互场景——在大多数简单场景中，静态背景信息也有帮助（L2 差异不大），但在关键的动态交互场景中，去除静态噪声带来的信噪比提升至关重要。如果 query 相减只是等价变换，理论上标准世界模型应能在相同训练条件下自行学会差分操作（因为 $Q_t - Q_{t-1}$ 是 $[Q_t, Q_{t-1}]$ 的线性组合，神经网络可以轻松学习），但实验否定了这一点。这一现象在架构对比中得到广泛佐证：TDN（Wang et al., CVPR 2021）用帧差分提取运动特征在动作识别中显著优于传统方法；Delta Network（O'Connor & Welling, 2016）和事件相机（Gallego et al., 2020）通过仅处理变化信号实现了 10-100 倍的数据压缩；ResAD（CVPR 2026）在自动驾驶轨迹预测中独立验证了残差表征的有效性（NAVSIM PDMS = 88.6）。从信息瓶颈理论（Tishby et al., 1999; Tishby & Zaslavsky, 2015）的角度，差分操作构成了一种面向动态预测的近似充分统计量——它丢弃了与预测目标无关的信息（$I(\Delta Q_t; Y_{static}) \ll I(Q_t; Y_{static})$），同时保留了相关信息（$I(\Delta Q_t; Y_{dynamic}) \approx I(Q_t; Y_{dynamic})$），从而在信息瓶颈意义上提高了表征效率。

- **形式化视角**
  - 变换 $\mathcal{T}: \mathbb{R}^{(T+1) \times d} \to \mathbb{R}^{(T+1) \times d}$，$(Q_0, Q_1, \ldots, Q_T) \mapsto (Q_0, \Delta Q_1, \ldots, \Delta Q_T)$
  - $\mathcal{T}$ 是线性的、双射的、保体积的（$|\det| = 1$），但**非正交**（$\mathcal{T}^T \mathcal{T} \neq I$）
  - 在优化理论框架中，$\mathcal{T}$ 的作用等价于一个**预处理算子**（preconditioner），改变了损失函数在参数空间中的曲率分布
  - 在信号处理框架中，$\mathcal{T}$ 是传递函数 $H(z) = 1 - z^{-1}$ 的高通滤波器，截止特性由差分阶数决定
  - 在预测编码框架中，$\mathcal{T}$ 是使用零阶保持预测的预测误差计算，承载条件信息 $H(X_t | X_{t-1})$

- **判定结论**
  - 命题"query 相减只是对 query 的等价变换"：**信息论层面部分成立，实质层面不成立**。
  - 结论强度：**强**——三个独立调研方向（ML 优化理论、信号处理/预测编码、架构对比/消融证据）交叉验证，结论高度收敛，无冲突。

- **变化 / 创新 / 贡献总结**
  - 从"完整场景表征"走向"时序残差表征"——将模型容量从描述完整状态重定向到描述状态变化。
  - 从"隐式学习忽略静态背景"走向"显式通过差分消除静态背景"——将任务先验从需要学习的软约束变为架构硬编码的归纳偏置。
  - 从"等价变换"的框架走向"信息选择性重构"的框架——更精确地描述差分操作在世界模型中的实际作用。

- **后续价值**
  - **系统设计启发**：在任何时序预测任务中，如果目标信号在时间上高度相关且任务聚焦于变化部分，差分/残差表征都是有效的架构设计选择。关键在于确保坐标对齐（或基线减去）的精度。
  - **可能的改进方向**：(1) 用可学习的差分函数 $f(Q_t, Q_{t-1})$ 替代线性相减；(2) 多尺度差分（同时使用 $Q_t - Q_{t-1}$ 和 $Q_t - Q_{t-k}$）；(3) 自适应预测模型替代零阶保持预测，逼近卡尔曼新息的白噪声特性。
  - **可迁移场景**：视频预测、机器人操控、金融时序预测等任何"静态冗余 + 动态预测"的任务。

---

**核心引用**（按调研方向分组）：

ML 优化理论：Amari (1998, Neural Computation); Santurkar et al. (2018, NeurIPS, arXiv:1805.11604); Salimans & Kingma (2016, NeurIPS, arXiv:1602.07868); Neyshabur et al. (2015, NeurIPS, arXiv:1506.02617).

信号处理与预测编码：Oppenheim & Schafer (2010, Prentice Hall); Rao & Ballard (1999, Nature Neuroscience); Friston (2005, Phil. Trans. R. Soc. B; 2010, Nature Reviews Neuroscience); Kalman (1960, J. Basic Engineering); Kailath (1968, IEEE Trans. AC); Barlow (1961, Sensory Communication, MIT Press).

架构对比与消融证据：He et al. (2016a, CVPR); Li et al. (2018, NeurIPS); Veit et al. (2016, NeurIPS); Wang et al. (2021, CVPR, TDN); Gallego et al. (2020, IEEE TPAMI, Event Camera); Tishby et al. (1999); Tishby & Zaslavsky (2015, IEEE ITW); TR-World (ICLR 2026, arXiv:2602.10884); ResAD (CVPR 2026, arXiv:2510.08562).

---

*本笔记基于三路并行子 agent 调研（ML 重参数化与优化景观 / 信号处理与预测编码 / 架构对比与消融证据）的交叉验证结果。详细调研记录保留在 `tmp/01-残差表征等价性问题研究/` 目录下。*
